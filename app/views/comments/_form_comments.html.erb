<%= form_for [@article, @comment] do |f| %>
  <% if !current_user %>
    <%= f.label :commenter, "Your Name:" %><br><br>
    <%= f.text_field :commenter %><br>
  <% end %>

  <%= f.label :body, "Comment:" %><br><br>
  <%= f.text_area :body %><br>

  <%= f.submit 'Post' %> <br><br>
<% end %>
